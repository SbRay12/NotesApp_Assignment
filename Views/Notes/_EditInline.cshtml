@* @model NotesApp.Models.Note

<form asp-controller="Notes" asp-action="Edit" method="post">
    <input type="hidden" name="Id" value="@Model.Id" />
    <div class="mb-2">
        <input name="Title" value="@Model.Title" class="form-control" maxlength="200" required />
    </div>
    <div class="mb-2">
        <textarea name="Content" class="form-control" rows="4" required>@Model.Content</textarea>
    </div>
    <div class="d-flex gap-2">
        <select name="Priority" class="form-select w-auto">
            <option value="0" @(Model.Priority == NotesApp.Models.Priority.Low ? "selected" : "")>Low</option>
            <option value="1" @(Model.Priority == NotesApp.Models.Priority.Medium ? "selected" : "")>Medium</option>
            <option value="2" @(Model.Priority == NotesApp.Models.Priority.High ? "selected" : "")>High</option>
        </select>
        <button class="btn btn-sm btn-primary">Save</button>
        <button type="button" class="btn btn-sm btn-secondary" onclick="toggleEdit(@Model.Id)">Cancel</button>
    </div>
    @Html.AntiForgeryToken()
</form> *@
